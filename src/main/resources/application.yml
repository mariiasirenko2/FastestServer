spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    platform: postgres
    url: jdbc:postgresql://127.0.0.1:5432/FasTest
    username: postgres
    password: 34702
    driver-class-name: org.postgresql.Driver
